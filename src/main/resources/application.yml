spring:
  datasource:
    # ✅ Neon strictly requires SSL
    url: jdbc:postgresql://ep-solitary-sun-a14i74ia-pooler.ap-southeast-1.aws.neon.tech/neondb?ssl=true&sslmode=require
    username: neondb_owner
    password: npg_gDtq3fhTRb5U
    driver-class-name: org.postgresql.Driver

  jpa:
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
        # ✅ Prevents metadata lookup hangs on startup
        temp:
          use_jdbc_metadata_defaults: false

  sql:
    init:
      mode: always

server:
  port: ${PORT:8081}

ai:
  brain:
    base-url: https://mrbaddy-student-life-os-ai-docker.hf.space
    connect-timeout-ms: 10000 
    read-timeout-ms: 120000    # ✅ Increased to 2 mins for heavy AI generation
    max-retries: 3
    timeout-ms: 120000
    circuit-breaker:
      failure-threshold: 5
      recovery-timeout-ms: 60000

cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:5173,https://mavisai.vercel.app}
